services:
  - type: web
    name: daily-digest
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -w 4 -b 0.0.0.0:$PORT web.app:app
    envVars:
      - key: OPENAI_API_KEY
        sync: false
